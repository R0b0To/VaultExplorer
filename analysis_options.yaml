include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - vaultexplorer-i18n-wip/**
    - lib/l10n/generated/**
    - build/**
    - android/**

plugins:
  riverpod_lint: ^3.1.8

linter:
  rules:
    avoid_print: true
    unawaited_futures: true
